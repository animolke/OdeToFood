@page
@model OdeToFood.Pages.Restaurants.ListModel
@{
    ViewData["Title"] = "Restaurants ";
}

<h1>Restaurants</h1>

<form method="get">
    <div class="form-group">
        <div class="input-group">
            <input type="search" class="form-control" asp-for="SearchTerm" />
            <span class="input-group-append">
                <button class="btn btn-outline-success">
                    Search
                </button>
            </span>
        </div>
    </div>
</form>

@*<table class="table">
    @foreach (var restaurant in Model.Restaurants)
    {
    <tr>
        <td>@restaurant.Name</td>
        <td>@restaurant.Location</td>
        <td>@restaurant.Cuisine</td>
        <td>
            <a asp-page="./Details" asp-route-RestaurantId="@restaurant.Id" class="btn btn-info">Details</a>
            <a asp-page="./Edit" asp-route-RestaurantId="@restaurant.Id" class="btn btn-primary">Edit</a>
            <a asp-page="./Delete" asp-route-RestaurantId="@restaurant.Id" class="btn btn-primary">Delete</a>
        </td>
    </tr>
    }
</table>*@

@foreach (var restaurant in Model.Restaurants)
{
    <partial name="_SummaryPartial" model="restaurant" />
}


<a asp-page="./Edit" class="btn btn-warning">Add New</a>

@section footer {
<p>Hello Footer</p>
}
